/**
 * En él se encuentran las clases relacionadas con la conexión del
 * programa a una Base de Datos, en específico a la Base de Datos Digimon,
 * de donde sacamos, añadimos y modificamos todos los datos que necesitamos.
 */
package conexion;
